$(document).ready(function(){      $('.goform-btn').click(function(e){         e.preventDefault();         var target = $(this).attr('href');         $('html, body').animate({scrollTop: $(target).offset().top}, 800);         return false;     });          //Timer 24 hours with cookie     var timerEndDate = null;     var myDate = new Date();      function returnEndDate(h,m,s){         myDate.setHours(myDate.getHours()+h);         myDate.setMinutes(myDate.getMinutes()+m);         myDate.setSeconds(myDate.getSeconds()+s);         return myDate;     }      if($.cookie("timer")){         var timerEndDate = $.cookie("timer");     }     else{         var timerEndDate = returnEndDate(18,45,50);         $.cookie("timer", timerEndDate, {expires: 36*60+15});     }      function zero(number) {         return (number < 10 ? '0' : '') + number;     }     function updateWCTime() {         now = new Date();         kickoff = Date.parse(timerEndDate);         diff = kickoff - now;         if (diff > 0) {             hours = Math.floor( diff / (1000*60*60) );             mins = Math.floor( diff / (1000*60) );             secs = Math.floor( diff / 1000 );             hh = hours;             mm = mins - hours * 60;             ss = secs - mins * 60;             $('.site_timer_hour').text(zero(hh));             $('.site_timer_min').text(zero(mm));             $('.site_timer_sec').text(zero(ss));         }         else{             clearInterval(timer);             return false;         }     }     var timer = setInterval(function(){updateWCTime()}, 500 );          numberprod(9);      });